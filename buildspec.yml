version: 0.2

phases:
  install:
    commands:
      - apt-get update && apt-get install -y zip
  pre_build:
    commands:
      - cp -r /home/a ../
  build:
    commands:
      - composer install
      - zip -r archive.zip ./
#  post_build:

artifacts:
  files:
    - archive.zip